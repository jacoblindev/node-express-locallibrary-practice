<!DOCTYPE html>
<html lang="en">
<%- include('partials/head'); -%>

    <body>
        <div class="container-fluid">
            <div class="row">
                <div class="col-sm-2">
                    <%- include('partials/sidenav'); -%>
                </div>
                <div class="col-sm-10">
                    <!-- Contents start here... -->
                    <h1>
                        <%= title %>: <%= bookinstance._id %>
                    </h1>
                    <p><strong>Do you really want to delete this instance?</strong></p>
                    <form action="" method="post">
                        <div class="form-group">
                            <input type="hidden" id="bookinstanceid" class="form-control" name="bookinstanceid" required
                                value="<%= bookinstance._id %>">
                        </div>
                        <button class="btn btn-primary" type="submit">Delete</button>
                    </form>
                    <!-- Contents end here!!! -->
                </div>
            </div>
        </div>
    </body>

</html>